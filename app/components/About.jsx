var React = require('react');

var About = (props) => {
    return(
      <div>
        <h2>About component</h2>,
        <p>Welcome to the component page</p>
      </div>
    );
};

module.exports = About;
